#!/usr/bin/env bash
# Simple xstartup for LXDE under VNC/Xvfb
xrdb "$HOME/.Xresources" >/dev/null 2>&1 || true
export XDG_RUNTIME_DIR="/tmp/xdg-runtime-$(id -u)"
mkdir -p "$XDG_RUNTIME_DIR"
chmod 700 "$XDG_RUNTIME_DIR"
lxsession &
