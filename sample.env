# sample.env
#
# Copy this to .env and customize as needed.
# The install script will auto-generate .env from this file if .env is missing.

# --- Build-time passwords (applied during image build) ---
# Changing these requires a rebuild (docker compose up --build)
USER_PASSWORD=changeme
ROOT_PASSWORD=changeme

# --- Runtime VNC Settings ---
# Note: Xvfb does not use a "refresh rate" concept like a physical display.
VNC_PASSWORD=changeme
VNC_GEOMETRY=1920x1080
VNC_DEPTH=24
