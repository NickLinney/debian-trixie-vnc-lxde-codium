# VNC Settings
# Note: Xvfb does not use a "refresh rate" concept like a physical display.
VNC_PASSWORD=yourpassword
VNC_GEOMETRY=1920x1080
VNC_DEPTH=24

# User / root passwords (applied at build time)
USER_PASSWORD=youruserpassword
ROOT_PASSWORD=yourrootpassword
